{
  "name": "samsarix-hub-directory",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "description": "A curated, dependency-free directory for the Samsarix ecosystem.",
  "license": "BUSL-1.1",
  "author": "Samsarix LLC <contact@samsarix.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/Deathcharge/Helix-Unified-Hub.git"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm test && npm run build",
    "lint": "node scripts/check.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
