# Portal prototypes

These pages were formerly stored at the repository root. They are preserved verbatim as historical prototypes and are not part of the GitHub Pages release artifact.

Some pages load mutable third-party scripts or describe unimplemented production services. Do not deploy them without a separate security and product review.
