# Incomplete backend manifest

These files were moved from the repository root because the Dockerfile references `backend/`, `Shadow/`, and `scripts/` directories that are not present here. The dependency list appears to have been copied from a larger Helix backend and is not required by Helix Hub Directory.

They are retained only as historical evidence. The image does not build from this repository and must not be presented as a supported deployment path.
